<ui:FluentWindow x:Class="WpfApp2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Steam Hour Booster" 
        Height="800" Width="1280"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized" 
        ExtendsContentIntoTitleBar="True">

    <ui:FluentWindow.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <SolidColorBrush x:Key="BgDark" Color="#181818"/>
        <SolidColorBrush x:Key="BgPanel" Color="#202020"/>
        <SolidColorBrush x:Key="BgCard" Color="#2d2d2d"/>
        <SolidColorBrush x:Key="AccentColor" Color="#4cc2ff"/>
        <SolidColorBrush x:Key="BoostColor" Color="#2d8f38"/>
        <SolidColorBrush x:Key="TextMuted" Color="#888888"/>
        <DropShadowEffect x:Key="ShadowEffect" BlurRadius="40" ShadowDepth="10" Opacity="0.5" Color="Black"/>

        <Style x:Key="SidebarBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="15" Width="30" Height="30">
                            <ui:SymbolIcon Symbol="Dismiss24" FontSize="16" Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#c42b1c"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ContextMenu}">
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ContextMenu}">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="5">
                            <StackPanel IsItemsHost="True"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </ui:FluentWindow.Resources>

    <Grid Background="{StaticResource BgDark}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource BgPanel}" BorderBrush="#333" BorderThickness="0,0,1,0">
            <Grid x:Name="SidebarGrid" Width="60">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Button Click="ToggleSidebar_Click" Height="40" Margin="10" Background="Transparent" BorderThickness="0">
                    <ui:SymbolIcon Symbol="Navigation24" FontSize="20" Foreground="White"/>
                </Button>

                <StackPanel Grid.Row="1" VerticalAlignment="Top">
                    <Button Click="NavToLibrary_Click" Style="{StaticResource SidebarBtn}">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Home24" FontSize="20" Margin="0,0,15,0" Foreground="White"/>
                            <TextBlock Text="Library" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                    <Button Click="RefreshLibrary_Click" Style="{StaticResource SidebarBtn}">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="ArrowSync24" FontSize="20" Margin="0,0,15,0" Foreground="White"/>
                            <TextBlock Text="Refresh Games" VerticalAlignment="Center" FontSize="14" />
                        </StackPanel>
                    </Button>
                    <Button Click="OpenProfile_Click" Style="{StaticResource SidebarBtn}">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Person24" FontSize="20" Margin="0,0,15,0" Foreground="White"/>
                            <TextBlock Text="My Profile" VerticalAlignment="Center" FontSize="14" />
                        </StackPanel>
                    </Button>
                </StackPanel>

                <StackPanel Grid.Row="2" VerticalAlignment="Bottom" Margin="0,0,0,20">
                    <Button Click="OpenTutorial_Click" Style="{StaticResource SidebarBtn}">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="QuestionCircle24" FontSize="20" Margin="0,0,15,0" Foreground="{StaticResource AccentColor}"/>
                            <TextBlock Text="Help / Tutorial" VerticalAlignment="Center" FontSize="14" Foreground="{StaticResource AccentColor}"/>
                        </StackPanel>
                    </Button>
                    <Button Click="NavToSettings_Click" Style="{StaticResource SidebarBtn}">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Settings24" FontSize="20" Margin="0,0,15,0" Foreground="White"/>
                            <TextBlock Text="Settings" VerticalAlignment="Center" FontSize="14" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <ui:TitleBar Grid.Row="0" Title="Steam Hour Booster" />

            <Grid x:Name="LibraryView" Grid.Row="1" Visibility="Visible">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="30,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ui:TextBox x:Name="SearchBox" PlaceholderText="Search games..." TextChanged="Filter_TextChanged" Margin="0,0,15,0" VerticalAlignment="Center" Height="35"/>
                    <ComboBox x:Name="SortComboBox" Grid.Column="1" Width="160" Margin="0,0,15,0" SelectionChanged="Sort_SelectionChanged" SelectedIndex="0" VerticalAlignment="Center" Height="35">
                        <ComboBoxItem Content="Name (A-Z)"/>
                        <ComboBoxItem Content="Name (Z-A)"/>
                        <ComboBoxItem Content="Time Played (High)"/>
                        <ComboBoxItem Content="Time Played (Low)"/>
                    </ComboBox>
                    <Button Grid.Column="2" Content="Filters" Click="FilterMenu_Click" Height="35" VerticalAlignment="Center" Padding="15,0">
                        <Button.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Show Hidden Games" IsCheckable="True" Click="Filter_Checked"/>
                                <MenuItem Header="Show Utilities / Tools" IsCheckable="True" Click="Filter_Checked"/>
                            </ContextMenu>
                        </Button.ContextMenu>
                    </Button>
                </Grid>

                <ScrollViewer Grid.Row="1" Padding="0,0,0,30">
                    <ListBox x:Name="GamesList" Background="Transparent" BorderThickness="0" 
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             HorizontalContentAlignment="Center">

                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Focusable" Value="False"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <ContentPresenter />
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>

                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel IsItemsHost="True" HorizontalAlignment="Center" MaxWidth="1600"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Button Click="Game_Click" Width="170" Height="270" Margin="10" Padding="0" BorderThickness="0" Cursor="Hand" Background="Transparent">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Grid>
                                                <Border x:Name="CardBorder" CornerRadius="24" Background="{StaticResource BgCard}" BorderBrush="Transparent" BorderThickness="1">

                                                    <Border.OpacityMask>
                                                        <VisualBrush>
                                                            <VisualBrush.Visual>
                                                                <Border Width="170" Height="270" CornerRadius="24" Background="Black"/>
                                                            </VisualBrush.Visual>
                                                        </VisualBrush>
                                                    </Border.OpacityMask>

                                                    <Grid>
                                                        <Image Source="{Binding ImageUrl}" Stretch="UniformToFill"/>

                                                        <Border VerticalAlignment="Bottom" Height="130">
                                                            <Border.Background>
                                                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                                    <GradientStop Color="#00000000" Offset="0"/>
                                                                    <GradientStop Color="#A0000000" Offset="0.5"/>
                                                                    <GradientStop Color="#E6000000" Offset="1"/>
                                                                </LinearGradientBrush>
                                                            </Border.Background>
                                                        </Border>

                                                        <Border BorderBrush="{StaticResource BoostColor}" BorderThickness="4" CornerRadius="24" Margin="-1"
                                                                Visibility="{Binding IsBoosting, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                Panel.ZIndex="10"/>

                                                        <Grid VerticalAlignment="Top" Margin="10" Panel.ZIndex="11">
                                                            <StackPanel Orientation="Horizontal">
                                                                <Border Background="#D9000000" CornerRadius="8" Padding="8,4" 
                                                                        Visibility="{Binding PlaytimeMinutes, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                                    <TextBlock Text="{Binding PlaytimeCompactString}" Foreground="White" FontSize="11" FontWeight="Bold"/>
                                                                </Border>
                                                                <Border Background="{StaticResource BoostColor}" CornerRadius="8" Padding="8,4" Margin="5,0,0,0"
                                                                        Visibility="{Binding IsBoosting, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                                    <TextBlock Text="RUNNING" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                                                </Border>
                                                            </StackPanel>
                                                            <Border HorizontalAlignment="Right" Background="#D9000000" CornerRadius="8" Padding="6" Visibility="{Binding IsHidden, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                                <ui:SymbolIcon Symbol="EyeOff24" Foreground="#ccc" FontSize="14"/>
                                                            </Border>
                                                        </Grid>

                                                        <TextBlock Text="{Binding Name}" 
                                                                   Foreground="White" 
                                                                   FontSize="14" 
                                                                   FontWeight="SemiBold"
                                                                   VerticalAlignment="Bottom" 
                                                                   HorizontalAlignment="Left" 
                                                                   TextTrimming="CharacterEllipsis" 
                                                                   Margin="15,0,15,15"
                                                                   Panel.ZIndex="12"/>
                                                    </Grid>
                                                </Border>
                                            </Grid>

                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="CardBorder" Property="Effect">
                                                        <Setter.Value>
                                                            <DropShadowEffect BlurRadius="25" ShadowDepth="0" Color="White" Opacity="0.25"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Setter TargetName="CardBorder" Property="RenderTransform">
                                                        <Setter.Value>
                                                            <ScaleTransform ScaleX="1.01" ScaleY="1.01" CenterX="85" CenterY="135"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </ScrollViewer>
            </Grid>

            <Grid x:Name="SettingsView" Grid.Row="1" Visibility="Collapsed" Margin="40">
                <StackPanel MaxWidth="700" HorizontalAlignment="Center">
                    <TextBlock Text="Settings" FontSize="32" FontWeight="Bold" Margin="0,0,0,30"/>
                    <ui:CardExpander Header="Steam API &amp; Connection" IsExpanded="True" Margin="0,0,0,20">
                        <StackPanel Margin="0,15,0,0">
                            <TextBlock Text="Steam Web API Key" Foreground="{StaticResource TextMuted}" Margin="0,0,0,5"/>
                            <ui:TextBox x:Name="TxtApiKey" PlaceholderText="Paste API Key here..."/>
                            <TextBlock Text="SteamID64" Foreground="{StaticResource TextMuted}" Margin="0,15,0,5"/>
                            <ui:TextBox x:Name="TxtSteamId" PlaceholderText="e.g., 76561198000000000"/>
                            <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                                <Button Content="Verify &amp; Save" Click="SaveSettings_Click" Style="{StaticResource PrimaryButtonStyle}" Width="150"/>
                                <TextBlock x:Name="TxtApiStatus" Text="Not Checked" VerticalAlignment="Center" Margin="15,0,0,0" Foreground="{StaticResource TextMuted}"/>
                            </StackPanel>
                        </StackPanel>
                    </ui:CardExpander>
                    <ui:CardExpander Header="Updates" IsExpanded="True">
                        <Button Content="Check GitHub for Updates" Click="GitHubUpdate_Click" Margin="0,10,0,0"/>
                    </ui:CardExpander>
                </StackPanel>
            </Grid>
        </Grid>

        <Grid x:Name="OptionsOverlay" Grid.ColumnSpan="2" Visibility="Collapsed" Background="#aa000000" Panel.ZIndex="1000">
            <Border MouseDown="CloseOverlay_Click" Background="Transparent"/>
            <Border Width="550" Height="500" Background="{StaticResource BgPanel}" CornerRadius="20" Effect="{StaticResource ShadowEffect}">
                <Grid Margin="30">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid>
                        <StackPanel>
                            <TextBlock x:Name="SelectedGameTitle" Text="Game Title" FontSize="24" FontWeight="Bold" Foreground="White"/>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <TextBlock x:Name="SelectedGameTime" Text="1050 hours" Foreground="{StaticResource AccentColor}" FontWeight="Bold" Margin="0,0,15,0"/>
                                <TextBlock x:Name="SelectedGameId" Text="ID: 12345" Foreground="{StaticResource TextMuted}"/>
                            </StackPanel>
                        </StackPanel>
                        <Button Click="CloseOverlay_Click" Style="{StaticResource CloseButtonStyle}" HorizontalAlignment="Right" VerticalAlignment="Top"/>
                    </Grid>
                    <StackPanel Grid.Row="1" VerticalAlignment="Center">
                        <Button Click="PlayGame_Click" Height="50" Margin="0,0,0,10" Background="#2d8f38" Foreground="White" BorderThickness="0">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="Play24" Margin="0,0,10,0"/>
                                <TextBlock Text="PLAY ON STEAM" FontWeight="Bold"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="BtnBoost" Click="BoostGame_Click" Height="50" Margin="0,0,0,10" Background="{StaticResource AccentColor}" Foreground="Black" BorderThickness="0">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="Rocket24" Margin="0,0,10,0"/>
                                <TextBlock x:Name="TxtBoost" Text="START IDLE BOOST" FontWeight="Bold"/>
                            </StackPanel>
                        </Button>
                        <Grid Margin="0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Content="Open Folder" Click="OpenFolder_Click" Height="35" Margin="0,0,5,0"/>
                            <Button Content="Repair Files" Click="RepairGame_Click" Grid.Column="1" Height="35" Margin="5,0,0,0" Background="#c42b1c" Foreground="White"/>
                        </Grid>
                        <TextBlock Text="⚠ Hint: If the game doesn't launch, click 'Repair Files' to restore the real exe." Foreground="#aaa" FontSize="11" TextWrapping="Wrap" TextAlignment="Center" Margin="0,10"/>
                    </StackPanel>
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                        <CheckBox x:Name="ChkHideGame" Content="Hide from Library" Checked="GameSetting_Changed" Unchecked="GameSetting_Changed" Margin="0,0,20,0"/>
                        <CheckBox x:Name="ChkIsUtility" Content="Mark as Utility" Checked="GameSetting_Changed" Unchecked="GameSetting_Changed"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Grid x:Name="TutorialOverlay" Grid.ColumnSpan="2" Visibility="Collapsed" Background="#aa000000" Panel.ZIndex="1001">
            <Border MouseDown="CloseOverlay_Click" Background="Transparent"/>
            <Border Width="650" Height="500" Background="{StaticResource BgPanel}" CornerRadius="20" Effect="{StaticResource ShadowEffect}">
                <Grid Margin="40">
                    <Button Click="CloseOverlay_Click" Style="{StaticResource CloseButtonStyle}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-20,-20,0"/>
                    <StackPanel>
                        <TextBlock Text="Quick Guide" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,30"/>
                        <TextBlock Text="1. Scan Library" FontWeight="Bold" FontSize="18" Foreground="{StaticResource AccentColor}"/>
                        <TextBlock Text="Click 'Refresh Games' to find installed games. Enter API Key in settings for real hours." Margin="0,0,0,20" Foreground="#ccc"/>
                        <TextBlock Text="2. Boost Hours" FontWeight="Bold" FontSize="18" Foreground="{StaticResource AccentColor}"/>
                        <TextBlock Text="Click 'Start Boost'. This renames your game exe and runs a lightweight dummy." Margin="0,0,0,20" Foreground="#ccc"/>
                        <TextBlock Text="3. Safety" FontWeight="Bold" FontSize="18" Foreground="{StaticResource AccentColor}"/>
                        <TextBlock Text="Always 'Stop Boost' before playing. If stuck, use 'Repair Files'." Margin="0,0,0,20" Foreground="#ccc"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                            <Button Content="Got it" Click="CloseOverlay_Click" Style="{StaticResource PrimaryButtonStyle}" Width="150" Margin="0,0,10,0"/>
                            <Button Content="Visit GitHub" Click="OpenGitHub_Click" Width="150"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Grid x:Name="NewGamesOverlay" Grid.ColumnSpan="2" Visibility="Collapsed" Background="#aa000000" Panel.ZIndex="1002">
            <Border MouseDown="CloseOverlay_Click" Background="Transparent"/>
            <Border Width="450" MaxHeight="500" Background="{StaticResource BgPanel}" CornerRadius="20" BorderBrush="#2d8f38" BorderThickness="2" Effect="{StaticResource ShadowEffect}">
                <Grid Margin="25">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
                        <ui:SymbolIcon Symbol="CheckmarkCircle24" Foreground="#2d8f38" FontSize="28" Margin="0,0,10,0"/>
                        <TextBlock Text="Scan Complete" FontSize="22" FontWeight="Bold"/>
                    </StackPanel>
                    <ListBox x:Name="NewGamesList" Grid.Row="1" Background="Transparent" BorderThickness="0" Margin="0,0,0,15" Foreground="White">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Button Click="Game_Click" Width="170" Height="270" Margin="10" Padding="0" BorderThickness="0" Cursor="Hand" Background="Transparent">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Grid x:Name="RootGrid">
                                                <Border x:Name="CardBorder" CornerRadius="24" Background="{StaticResource BgCard}" ClipToBounds="True">
                                                    <Grid>
                                                        <Image Source="{Binding ImageUrl}" Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality"/>

                                                        <Border VerticalAlignment="Bottom" Height="130">
                                                            <Border.Background>
                                                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                                    <GradientStop Color="#00000000" Offset="0"/>
                                                                    <GradientStop Color="#A0000000" Offset="0.5"/>
                                                                    <GradientStop Color="#E6000000" Offset="1"/>
                                                                </LinearGradientBrush>
                                                            </Border.Background>
                                                        </Border>

                                                        <Border x:Name="BoostRing" BorderBrush="{StaticResource BoostColor}" BorderThickness="4" CornerRadius="24" 
                                        Visibility="{Binding IsBoosting, Converter={StaticResource BooleanToVisibilityConverter}}"
                                        Panel.ZIndex="10"/>

                                                        <Grid VerticalAlignment="Top" Margin="10" Panel.ZIndex="11">
                                                            <StackPanel Orientation="Horizontal">
                                                                <Border Background="#D9000000" CornerRadius="8" Padding="8,4" 
                                                Visibility="{Binding PlaytimeMinutes, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                                    <TextBlock Text="{Binding PlaytimeCompactString}" Foreground="White" FontSize="11" FontWeight="Bold"/>
                                                                </Border>
                                                                <Border Background="{StaticResource BoostColor}" CornerRadius="8" Padding="8,4" Margin="5,0,0,0"
                                                Visibility="{Binding IsBoosting, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                                    <TextBlock Text="RUNNING" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                                                </Border>
                                                            </StackPanel>
                                                        </Grid>

                                                        <TextBlock Text="{Binding Name}" Foreground="White" FontSize="14" FontWeight="SemiBold"
                                           VerticalAlignment="Bottom" HorizontalAlignment="Left" 
                                           TextTrimming="CharacterEllipsis" Margin="15,0,15,15" Panel.ZIndex="12"/>
                                                    </Grid>
                                                </Border>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="CardBorder" Property="Effect">
                                                        <Setter.Value>
                                                            <DropShadowEffect BlurRadius="25" ShadowDepth="0" Color="White" Opacity="0.15"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Setter TargetName="CardBorder" Property="RenderTransform">
                                                        <Setter.Value>
                                                            <TranslateTransform Y="-2"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <Button Grid.Row="2" Content="Excellent" Click="CloseOverlay_Click" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Center" Width="120"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</ui:FluentWindow>